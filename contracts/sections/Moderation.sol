pragma solidity ^0.4.23;

import "../imports/GenericStorage.sol";

contract Moderation is GenericStorage {
    function voteToRemoveProjectAdmin(
        address _org,
        uint256 _project
    ) public {
    }
}
